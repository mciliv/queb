// Stub: hotel is an independent page served separately
// Prompt: app.js line 116 requires { setupHotelRoutes } -- stub to prevent crash
function setupHotelRoutes(app, { logger }) {
  // Hotel routes are served as a standalone page via public/hotel/
}

module.exports = { setupHotelRoutes };
